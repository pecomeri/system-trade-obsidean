---
strategy: D001_m1_momentum_burst_range_only
filters:
  - W1 only
  - H1 filter ON (compute_h1_uptrend参照)
result: untested
---

# D001（family_D_momentum）baseline

## A/B/C と違う点（3行）

- A/B/C が「レベル（過去高値/安値）のブレイク」を前提にするのに対し、D001 は **M1確定足の“実体レンジ加速”**のみでトリガーする
- 10秒足は **執行補助（次の10秒open）**に限定し、トリガーは M1 の確定足で判定する
- H1 は既存の `compute_h1_uptrend` を事故抑制として参照する（定義は再発明しない）

## Filters（事故抑制）

- W1 only（時間帯制約）
  - `session_label(ts,cfg) == "W1"` のときだけ通す
- H1 filter ON
  - `compute_h1_uptrend(df10,cfg) == True` のときだけ通す（定義はcore参照）

## Strategy（ロジック）

- `FX/20_strategies/D001_m1_momentum_burst_range_only.md` を正典とする

## 実行

- verify: 2024年
- forward: 2025年
- 出力: `FX/results/family_D_momentum/D001/`

